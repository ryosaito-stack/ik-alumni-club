rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 開発環境用：すべて許可（本番環境では絶対に使用しないこと！）
    match /{document=**} {
      allow read, write: if true;
    }
  }
}